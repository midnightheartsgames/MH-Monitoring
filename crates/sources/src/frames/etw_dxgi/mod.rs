//! Собственный ETW-потребитель кадров — запасной источник (решение D1).
//!
//! Что он покрывает, измерено в P0 и записано в `spikes/etw-frames/COVERAGE.md`:
//! D3D9, D3D11, D3D12 и Vulkan видны, **OpenGL — нет**. Замеры сделаны на NVIDIA в оконном и
//! безрамочном режимах; за их пределами покрытие не проверялось.
//!
//! Поэтому при откате на этот источник пользователю сообщают про OpenGL, а не про Vulkan, как
//! предполагалось в PLAN.md §2.4 до измерения.
//!
//! Разделение внутри модуля то же, что у PresentMon: [`payload`] чистый и проверяется везде,
//! `session` работает с ETW и живёт только под Windows.

pub mod payload;

#[cfg(windows)]
pub mod session;
#[cfg(windows)]
pub mod source;
